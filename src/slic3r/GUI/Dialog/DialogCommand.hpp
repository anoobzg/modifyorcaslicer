#pragma once 

namespace Slic3r {
namespace GUI {
    void open_about_dialog(wxWindow* parent = nullptr);
    void open_keyboard_shortcuts_dialog(wxWindow* parent = nullptr);
    void open_network_test_dialog(wxWindow* parent = nullptr);
}
}